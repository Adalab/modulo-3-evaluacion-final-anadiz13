import FilterByName from FilterByName;

const Filters = (props) => {
    return (
        <section>
              <form className="form">
                <FilterByName></FilterByName>
              </form>
              </section>
            
    );
  };
  export default Filters;